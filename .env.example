# Absolute origin used for canonical URLs, the sitemap and social card images.
# Set this in Vercel to your production domain once it is live.
# Leave it unset locally and on preview deployments — the code falls back to
# VERCEL_URL so previews advertise their own URL rather than production's.
NEXT_PUBLIC_SITE_URL=https://your-domain.com
